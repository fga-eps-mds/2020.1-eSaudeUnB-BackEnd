version: '3.8'

services:

    e-saude-backend:
        build:
            context: .
            dockerfile: Dockerfile
        image: e-saude-backend
        ports:
            - 8000:8000
        volumes:
            - '.:/app'
            - '/app/node_modules'